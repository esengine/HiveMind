# HiveMind Server - Lightweight Dependencies
# For deployment on low-resource servers (2GB RAM)

# Server
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Utils
pydantic>=2.0.0
httpx>=0.25.0

# Optional: for aggregation (install separately when needed)
# pip install torch safetensors --index-url https://download.pytorch.org/whl/cpu
